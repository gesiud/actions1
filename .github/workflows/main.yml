name: CI

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: No build required
        run: echo "No build required; skipping."
